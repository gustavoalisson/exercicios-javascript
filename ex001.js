// usar variaveis

const calcula = (valor1, valor2) =>{
    let soma = console.log(`Soma: ${valor1 + valor2}`)
    let multiplc = console.log(`Multiplicação: ${valor1 * valor2}`)
    let subtr = console.log(`Subtração: ${valor1 - valor2}`)
    let divs = console.log(`Divisão: ${valor1 / valor2}`)
} 

calcula(2,2)
console.log('--------')
calcula(10,2)